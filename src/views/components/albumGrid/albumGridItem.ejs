<a href="/albums/<%= album._id  %>" class="album">
  <img
    src="<%= album.images.filename %> "
    alt="<%= album.title %> <%= album.subTitle %>"
  />
  <h6 class="album__title"><%= album.title %></h6>
  <p class=""><%= album.subTitle %></p>
</a>
